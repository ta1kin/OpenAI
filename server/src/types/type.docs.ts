export interface UserDoc {
    headline: string
    file: File
    date: Date
    formatDate: string
}

export interface ResLoadData {
    id: number
    formatDate: Date
}

export interface FormatDocs {
    id: number
    headline: string
    date: number
    formatDate: Date
}